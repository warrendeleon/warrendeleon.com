---
import BaseLayout from '../../layouts/BaseLayout.astro';

const locale = 'es' as const;

const openRoles = [
  {
    title: 'Software Engineer — React Native (Platform)',
    team: 'Mobile Platform, UCX-Core',
    location: 'Londres, Reino Unido (Híbrido)',
    url: 'https://hargreaveslansdown.wd3.myworkdayjobs.com/en-US/HargreavesLansdown/job/Software-Engineer--React-Native---Platform----UCX_R9031?locationCountry=29247e57dbaf46fb855b224e03170bc7',
    closingDate: '2026-04-26',
  },
];

const allRolesUrl = 'https://hargreaveslansdown.wd3.myworkdayjobs.com/en-US/HargreavesLansdown?locationCountry=29247e57dbaf46fb855b224e03170bc7&jobFamilyGroup=32c04e37b469014cf1d02d4fcd2301b1';
---

<BaseLayout
  title="Únete al equipo | Warren de Leon"
  description="Vacantes abiertas en el equipo de Mobile Platform en Hargreaves Lansdown. Roles de React Native en Londres, Reino Unido."
  locale={locale}
  currentPath="/es/hiring/"
>
  <section class="section">
    <div class="container">
      <h1 class="section-heading">Únete al equipo</h1>

      <div class="hiring-intro">
        <p>Estoy contratando para el squad de <strong>Mobile Platform</strong> en <strong>Hargreaves Lansdown</strong>. Estamos reconstruyendo la app móvil de HL en React Native, una migración brownfield desde las apps nativas de iOS y Android. El equipo de plataforma es dueño de la arquitectura, los componentes compartidos y los pipelines de CI/CD a través de los cuales todos los squads móviles hacen sus entregas.</p>
        <p>Si querés trabajar con React Native a escala en un equipo que valora la arquitectura limpia, el testing sólido y la verdadera ownership, echale un vistazo a los roles abiertos.</p>
      </div>

      {openRoles.length > 0 ? (
        <div class="hiring-roles">
          <h2 class="hiring-roles-title">
            <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
              <rect x="2" y="7" width="20" height="14" rx="2" ry="2"></rect>
              <path d="M16 21V5a2 2 0 0 0-2-2h-4a2 2 0 0 0-2 2v16"></path>
            </svg>
            Roles abiertos
          </h2>
          <div class="hiring-role-grid">
            {openRoles.map(role => (
              <a href={role.url} target="_blank" rel="noopener noreferrer" class="hiring-role-card" data-closing-date={role.closingDate}>
                <div class="hiring-role-content">
                  <h3 class="hiring-role-title">{role.title}</h3>
                  <span class="hiring-role-team">{role.team}</span>
                  <span class="hiring-role-location">{role.location}</span>
                </div>
                <span class="hiring-role-cta">
                  Aplicar ahora
                  <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                    <path d="M18 13v6a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h6"></path>
                    <polyline points="15 3 21 3 21 9"></polyline>
                    <line x1="10" y1="14" x2="21" y2="3"></line>
                  </svg>
                </span>
              </a>
            ))}
          </div>
        </div>
      ) : (
        <div class="hiring-empty">
          <p>No hay roles abiertos en mi equipo en este momento.</p>
        </div>
      )}

      <div class="hiring-all-roles">
        <p>¿Buscas otros roles de ingeniería en Hargreaves Lansdown?</p>
        <a href={allRolesUrl} target="_blank" rel="noopener noreferrer" class="hiring-all-roles-link">
          Ver todos los roles de desarrollo en HL
          <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
            <path d="M18 13v6a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h6"></path>
            <polyline points="15 3 21 3 21 9"></polyline>
            <line x1="10" y1="14" x2="21" y2="3"></line>
          </svg>
        </a>
      </div>
    </div>
  </section>
</BaseLayout>
<script>
  const today = new Date().toISOString().slice(0, 10);
  const cards = document.querySelectorAll('.hiring-role-card[data-closing-date]');
  let visibleCount = cards.length;

  cards.forEach(card => {
    const closing = card.getAttribute('data-closing-date');
    if (closing && today > closing) {
      card.remove();
      visibleCount--;
    }
  });

  if (visibleCount === 0) {
    const rolesSection = document.querySelector('.hiring-roles');
    const emptySection = document.querySelector('.hiring-empty');
    if (rolesSection) rolesSection.style.display = 'none';
    if (emptySection) emptySection.style.display = 'block';
  }
</script>
